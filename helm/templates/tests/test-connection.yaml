apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "k8s-ingress-ssl-metrics-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "k8s-ingress-ssl-metrics-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "k8s-ingress-ssl-metrics-exporter.fullname" . }}:{{ .Values.service.port }}/metrics']
  restartPolicy: Never