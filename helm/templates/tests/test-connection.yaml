apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ssl_checker.fullname" . }}-test-connection"
  labels:
    {{- include "ssl_checker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ssl_checker.fullname" . }}:{{ .Values.service.port }}/metrics']
  restartPolicy: Never