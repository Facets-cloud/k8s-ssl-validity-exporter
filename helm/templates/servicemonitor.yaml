apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "k8s-ingress-ssl-metrics-exporter.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
     {{- include "k8s-ingress-ssl-metrics-exporter.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "k8s-ingress-ssl-metrics-exporter.selectorLabels" . | nindent 5 }}
  endpoints:
  - targetPort: metrics
    port: metrics
    path: /metrics
    scrapeTimeout: 60s
    interval: 120s