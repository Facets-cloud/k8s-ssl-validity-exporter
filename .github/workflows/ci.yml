name: ci

on:
  push:
    branches:
      - "master"

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
#      - name: Build and push
#        uses: docker/build-push-action@v2
#        with:
#          context: .
#          file: ./build/Dockerfile
#          platforms: linux/amd64,linux/arm64
#          push: true
#          tags: facetscloud/k8s-ingress-ssl-metrics-exporter-test:${{ github.run_number }}

#      - name: Tag and push latest
#        run: |
#          docker tag facetscloud/k8s-ingress-ssl-metrics-exporter:${{ github.run_number }} facetscloud/k8s-ingress-ssl-metrics-exporter:latest
#          docker push facetscloud/k8s-ingress-ssl-metrics-exporter:latest